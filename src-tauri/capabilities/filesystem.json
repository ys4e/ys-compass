{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "filesystem",
  "description": "capabilities for accessing the file system",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open"
  ]
}
